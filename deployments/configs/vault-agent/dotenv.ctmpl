# Static Configuration
DB_HOST=db
DB_PORT=5432
REDIS_HOST=redis
REDIS_PORT=6379
MINIO_ENDPOINT=minio:9000
MINIO_BUCKET=places-social
MINIO_SECURE=false
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Vault-Managed Secrets
{{ with secret "secret/data/places-backend" }}
{{ range $k, $v := .Data.data }}
{{ $k }}={{ $v }}
{{ end }}
{{ end }}
